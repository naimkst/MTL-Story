/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["mtlstory-admin.outrivalmarkets.com"],
  },
  experimental: {
    appDir: false,
  },
};

module.exports = nextConfig;
